export type StatusType = {
  name: string;
  status: number;
};

export type RankingType = {
  userName: string;
  contribute: number;
};

export type HistoryType = {
  userName: string;
  action: string;
  time: string;
};
